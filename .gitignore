# ===================================================================
# 🌐 OMLMSC-VSBTWSL — Final Universal .gitignore
# Covers: C/C++, Rust, C#, Python, Go, Ruby, RedStone.Script, Zig, Julia, Nim, Swift,
#         WSL2, CI, IDEs, Docker, and — media assets (logos, screenshots, docs)
# ===================================================================

# ---------- Общие временные файлы ----------
*~
.*.swp
.DS_Store
desktop.ini
Thumbs.db
.cache/
.temp/

# ---------- Системные и логи ----------
*.log
*.tmp
*.bak
*.old
crash.dmp
debug.log

# ---------- Сборочные артефакты ----------
/build/
/out/
/dist/
/bin/
/lib/
/obj/
/tmp/
/staging/

# ---------- C / C++ / MSVC ----------
*.o *.obj *.a *.lib *.dll *.so *.dylib *.exe *.pdb *.ilk *.exp *.manifest *.map *.res
.vs/ *.sln *.suo *.vcxproj* ipch/ Debug/ Release/ x64/ x86/ ARM64/ ARM64EC/

# ---------- CMake & Ninja ----------
CMakeCache.txt CMakeFiles/ cmake_install.cmake *.ninja .ninja_log

# ---------- Rust ----------
/target/ **/*.rs.bk
# Cargo.lock — оставлен (коммитим для библиотек)

# ---------- C# / .NET ----------
/bin/ /obj/ *.csproj.user *.sln.ide *.user publish/ appsettings.Development.json

# ---------- Python ----------
__pycache__/ *.pyc *.pyo *.pyd .venv/ venv/ env/ .env* .pytest_cache/ .mypy_cache/ .pyre/

# ---------- Go ----------
/go/bin/ go.sum   # go.sum — оставлен (рекомендуется коммитить)

# ---------- Ruby / Julia / Zig / Nim / Swift ----------
.bundle/ vendor/bundle/ .ruby-version .julia/ zig-cache/ nimcache/ DerivedData/ *.xcodeproj/

# ---------- RedStone.Script 🌟 ----------
*.rsx *.rsc *.rslib *.rsobj /redstone_cache/ /redstone_build/

# ---------- Pixselltion OS ----------
*.vomf   # ← НЕ игнорится — это часть конфигурации/описания модулей

# ---------- IDE & Editors ----------
.vscode/           # кроме явно нужных: !.vscode/settings.json и т.д.
.idea/ *.iml *.ipr *.iws
*.sublime-* \#*\# tags TAGS

# ---------- CI / DevOps ----------
.github/workflows/*.tmp .gitlab-ci.yml azure-pipelines.yml

# ---------- Docker ----------
.dockerignore Dockerfile.tmp /docker/build/

# ---------- Секреты (защита) ----------
*.pem *.key *.cert *.crt id_rsa secrets.json credentials.json

# ---------- Графика: ВРЕМЕННЫЕ/АВТОГЕНЕРИРУЕМЫЕ файлы ----------
# ← ВАЖНО: статичные логотипы (logo.jpg, microsoft.jpg и т.п.) НЕ игнорятся — они в репо!
*.tmp.jpg
*.rendered.png
/temp-*.png
/screenshots/tmp_*
/docs/_build/
/docs/generated/
